# 终结性项目文档 · 本地闽菜食材筛选菜谱网站

---

## 一、项目基本信息

| 项目 | 内容 |
|------|------|
| **项目名称** | 本地闽菜食材筛选菜谱库（Min Cuisine Recipe Filter） |
| **学生姓名** | [请填写] |
| **学号** | [请填写] |
| **项目类型** | 纯前端静态 Web 应用 |
| **技术栈** | HTML5 + Tailwind CSS v3 + 原生 JavaScript (ES6+) |
| **开发周期** | 2026年6月9日（单日迭代完成） |
| **版本** | v1.3.0（最终版） |
| **代码规模** | 约 57KB 单文件，含 51 道闽菜完整数据 |

---

## 二、摘要

本项目构建了一个纯前端的闽菜食材筛选菜谱网站，用户可通过输入或选择食材，快速查找能制作的闽菜菜谱。项目以福州菜、闽南菜、闽西菜、莆仙菜四大流派的 51 道经典闽菜为基础数据，提供食材模糊匹配筛选、流派/难度/耗时多维组合筛选、菜谱详情弹窗展示等功能。

项目解决的核心问题是：家庭烹饪者面对冰箱里的食材不知道能做什么闽菜，需要一个快速、直观的「食材→菜谱」反向检索工具。验收指标包括：全量菜谱默认展示、食材实时筛选响应 <100ms、四维 AND 组合筛选、详情弹窗含完整步骤和用量、文化背景与外部链接。

项目达成全部 20 项功能需求，数据覆盖 51 道闽菜，每道菜含具体用量、详细步骤、烹饪贴士和文化背景，实现了从食材到菜谱、从菜谱到文化的完整信息链路。

---

## 三、项目背景与需求

### 3.1 问题描述
- 家庭烹饪者常面临「冰箱里有食材，但不知道能做什么菜」的困境
- 闽菜作为中国八大菜系之一，有丰富的菜谱资源，但缺乏按食材检索的便捷工具
- 现有菜谱网站多为列表浏览模式，不支持「反向检索」——从食材找菜谱

### 3.2 目标用户
- 家庭烹饪爱好者，希望根据现有食材快速找到能做闽菜
- 闽菜文化爱好者，希望了解闽菜的历史背景和烹饪技巧
- 厨房新手，需要详细的用量和步骤指导

### 3.3 使用场景
1. **食材驱动**：用户输入「五花肉」「海蛎」，系统列出所有能用这些食材做出的闽菜
2. **多维筛选**：用户想找「福州菜 + 简单难度 + 30分钟内」的菜
3. **学习参考**：用户对某道菜感兴趣，查看详细步骤、调味用量和历史文化

### 3.4 约束条件
- **纯前端**：不使用任何后端服务，所有数据存储于静态文件中
- **单文件**：HTML/CSS/JS 合并在一个文件中，方便分发和离线使用
- **零依赖**：除 Tailwind CSS CDN 外，不依赖任何框架或库
- **响应式**：适配手机、平板、桌面三种设备

---

## 四、方案与实现

### 4.1 总体架构

```
┌─────────────────────────────────────────────┐
│                  index.html                  │
│  ┌──────────┐ ┌──────────┐ ┌─────────────┐  │
│  │  HTML 结构 │ │ CSS 样式  │ │ JavaScript  │  │
│  │  (语义化)  │ │(Tailwind)│ │  (原生 ES6) │  │
│  └──────────┘ └──────────┘ └─────────────┘  │
│                      │                       │
│            ┌─────────▼──────────┐            │
│            │   recipes[] 数组    │            │
│            │  (51道闽菜 JSON)    │            │
│            └────────────────────┘            │
└─────────────────────────────────────────────┘
```

**数据流**：用户操作 → 状态更新 → filterRecipes() → 四维 AND 筛选 → DOM 渲染

### 4.2 模块说明

| 模块 | 职责 | 关键函数 |
|------|------|----------|
| **数据层** | 51 道菜数据存储（11 字段/道） | `recipes[]` 数组 |
| **筛选引擎** | 食材模糊匹配 + 三维属性筛选 | `filterRecipes()`, `ingredientMatches()`, `parseTimeToMinutes()` |
| **UI 渲染** | 卡片网格、标签、筛选栏、弹窗 | `renderRecipeCards()`, `renderFilterBar()`, `openDetail()` |
| **交互管理** | 点击、输入、键盘事件 | `toggleIngredient()`, `clearAllFilters()`, `closeModal()` |
| **自动补全** | 食材输入联想 | `updateAutoComplete()`, `collectAllIngredients()` |

### 4.3 关键技术选型与理由

| 技术 | 选择理由 |
|------|----------|
| **Tailwind CSS CDN** | 原子化 CSS，无需构建工具；CDN 引入零配置；自定义主题色支持 |
| **原生 JavaScript** | 无框架依赖，文件体积小，浏览器直接运行，无需编译 |
| **单文件架构** | 方便分发和离线使用，减少 HTTP 请求 |
| **数据内嵌** | 51 道菜数据量小（~57KB），内嵌比异步加载更简单可靠 |
| **双向模糊匹配** | `ri.includes(st) \|\| st.includes(ri)` ——「芋头」能匹配「槟榔芋头」，「猪肉」能匹配「猪肉馅」 |
| **时间解析正则** | `/(\d+)\s*小时/` + `/(\d+)\s*分钟/` 统一转为分钟数，适配「3小时」「90分钟」等混合格式 |

### 4.4 数据结构设计

每道菜包含 11 个字段：

| 字段 | 类型 | 示例 |
|------|------|------|
| `name` | string | 佛跳墙 |
| `style` | string | 福州菜 |
| `taste` | string | 清鲜醇厚、浓郁荤香 |
| `mainIngredients` | string | 鲍鱼 6只, 海参 4条, … |
| `seasoning` | string | 绍兴酒 100ml, 生姜 30g, … |
| `difficulty` | string | 困难 |
| `time` | string | 3小时 |
| `servings` | string | 6-8人份 |
| `tips` | string | 干货至少提前12小时分开泡发… |
| `story` | string | 佛跳墙创于清光绪年间… |
| `link` | string | https://baike.baidu.com/item/佛跳墙 |
| `steps` | string | 1.…；2.…；3.…（分号分隔步骤） |

---

## 五、Vibe Coding 过程证据

### 阶段 1：需求澄清

**用户初始提示词（关键轮次）**：

> 「帮我做一个『本地闽菜食材筛选菜谱网站』，要求一次性生成完整可运行代码，不使用后端，纯前端静态页面。技术栈：HTML + Tailwind CSS v3 + 原生 JavaScript。功能需求：标题区、食材输入区（手动输入+常用标签多选）、菜谱展示区（默认全量+实时筛选）、菜谱详情弹窗、闽菜红 #c8161d 配色。文件夹里已有 50 道闽菜 JSON。」

**规格化结果**：
- 功能需求：5 个主功能模块（F1-F5）
- 非功能需求：纯前端、单文件、响应式
- 验收指标：50 道菜全量展示、实时筛选、详情弹窗、移动端适配
- AI 先读取 recipe.json 确认数据结构，再生成完整 index.html

### 阶段 2：方案生成与架构

**对比方案**：

| 方案 | 优点 | 缺点 | 决策 |
|------|------|------|------|
| React/Vue SPA | 组件化、状态管理好 | 需构建工具、体积大、离线不便 | ❌ 否决 |
| 多文件 HTML+JS+CSS | 结构清晰 | 分发不便、HTTP 请求多 | ❌ 否决 |
| **单文件 HTML 内嵌一切** | 零配置、易分发、离线可用 | 文件较大 | ✅ 采用 |

**决策依据**：用户明确要求「纯前端」「不使用后端」「一次性生成完整可运行代码」，单文件 HTML 最能满足这些约束。Tailwind CDN 提供原子化 CSS 能力而无需本地构建。

### 阶段 3：实现迭代

共经历 5 轮主要迭代：

| 迭代 | 版本 | 主要变更 | 验收样例 |
|------|------|----------|----------|
| **第1轮** | v1.0.0 | 初始构建：HTML 结构 + 食材筛选 + 卡片 + 弹窗 | 默认显示 50 道菜 ✓，点击标签筛选 ✓ |
| **第2轮** | v1.1.0 | 数据增强：食材/调料加用量，步骤详细化，新增 servings/tips | 弹窗显示「五花肉 500g」✓，步骤含温度时间 ✓ |
| **第3轮** | — | 改进方向分析（纯思考，10 项改进，排优先级） | 识别多维筛选为最高优先级 ✓ |
| **第4轮** | v1.2.0 | 多维筛选：流派/难度/耗时 Chip 按钮，四维 AND 筛选 | 「福州菜+简单+快速」组合筛选结果正确 ✓ |
| **第5轮** | v1.3.0 | 文化深度：51 道菜 story 背景 + link 外链，弹窗新增区块 | 弹窗显示「📖 文化背景」✓，链接新标签页打开 ✓ |

每轮迭代的 commit 验收记录：见 `dev-log/changelog.md` 和 `dev-log/execution-steps.md`。

### 阶段 4：调试修复（完整案例）

**案例：文件损坏恢复（v1.3.0 实施过程中）**

| 步骤 | 内容 |
|------|------|
| **复现** | 在添加 51 道菜的 `story`/`link` 字段时，多次 Edit 操作导致 index.html 内容不完整——HTML 结构被截断，`<script>` 标签缺失 |
| **定位** | IDE 诊断报错 `']' expected` (line 146)；检查文件发现 `</footer>` 后无 `<script>` 标签，recipes 数组不存在 |
| **根因** | Write 工具覆盖文件时未包含完整 JS 部分，导致文件仅剩 HTML 结构（~100行），所有 JavaScript 逻辑丢失 |
| **修复方案** | 采用分步重组策略：<br>1. 用 Node.js 从已完成的 recipe.json 提取数据，生成 `_recipes.js`<br>2. 用 Write 工具将 JS 逻辑写入 `_tail.js`（避免 bash 转义问题）<br>3. 用 bash `cat` 拼接 HTML 头部 + recipes 数组 + JS 尾部 → 完整 index.html<br>4. 验证：文件头尾完整（DOCTYPE→/html）、51 个 recipe 对象、story/link 字段各 51 个、全部 5 个核心函数存在 |
| **测试** | Node.js 脚本逐项检查：recipes 数量、story 字段数、link 字段数、关键函数、DOM 元素 ID，全部通过 |
| **教训** | 大文件操作应优先使用脚本化批量处理，避免多次局部 Edit 导致的不一致状态；文件操作后立即验证完整性 |

---

## 六、结果与验收

### 功能需求验收清单（AC：Acceptance Criteria）

| # | 验收项 | 状态 | 说明 |
|---|--------|------|------|
| 1 | 顶部标题「闽菜食材菜谱库」+ 副标题 | ✅ | 含实时菜谱计数徽章 |
| 2 | 食材输入框 + 自动补全下拉 | ✅ | 支持手动输入，Enter/按钮添加 |
| 3 | 12 个常用食材标签 + 多选 | ✅ | 猪肉/鸡肉/虾仁/香菇/豆腐/芋头/花生/猪脚/鲈鱼/青菜/海蛎/鸡蛋 |
| 4 | 已选标签展示 + 单独删除 | ✅ | 红色标签带 × 删除按钮 |
| 5 | 默认显示全部 51 道菜 | ✅ | 未筛选时全量展示 |
| 6 | 食材实时筛选（模糊匹配） | ✅ | 双向包含匹配，响应即时 |
| 7 | 菜谱卡片展示完整信息 | ✅ | 菜名/流派/口味/食材/难度/耗时 |
| 8 | 匹配食材高亮 | ✅ | 命中的食材红色标签，其余灰色 |
| 9 | 详情弹窗（步骤 + 用量 + 调料） | ✅ | 步骤编号列表，食材/调料标签 |
| 10 | ESC/点击遮罩关闭弹窗 | ✅ | |
| 11 | 流派筛选（全部/福州/闽南/闽西/莆仙） | ✅ | Chip 按钮组，单选 |
| 12 | 难度筛选（全部/简单/中等/困难） | ✅ | Chip 按钮组，单选 |
| 13 | 耗时筛选（快速≤30min/适中30-60min/慢炖>60min） | ✅ | 基于 parseTimeToMinutes() 自动归类 |
| 14 | 四维 AND 组合筛选 | ✅ | 食材+流派+难度+耗时同时生效 |
| 15 | 清除筛选按钮 | ✅ | 一键重置四种筛选+食材 |
| 16 | 食材/调料标注用量 | ✅ | g/ml/个/只/根/少许 |
| 17 | 详细烹饪步骤（330+ 步） | ✅ | 含温度/时间/状态判断 |
| 18 | 份量信息（servings） | ✅ | 如 3-4人份 |
| 19 | 烹饪小贴士（tips） | ✅ | 黄色提示框 |
| 20 | 文化背景 + 外部链接 | ✅ | 蓝色卡片 + 百科外链按钮 |
| 21 | 响应式布局 | ✅ | 手机1列/平板2列/桌面3列 |
| 22 | 闽菜红 #c8161d 主色调 | ✅ | 按钮/标签/高亮统一使用 |
| 23 | 开发日志文件夹 | ✅ | 6个文档文件 |

### 数据覆盖

| 流派 | 数量 |
|------|------|
| 福州菜 | 18 道 |
| 闽南菜 | 16 道 |
| 闽西菜 | 9 道 |
| 莆仙菜 | 8 道 |
| **合计** | **51 道** |

---

## 七、复现指南

### 7.1 环境要求
- 操作系统：Windows / macOS / Linux 均可
- 浏览器：Chrome 90+, Firefox 90+, Safari 15+, Edge 90+
- 网络：首次加载需访问 CDN（Tailwind CSS + Google Fonts），后续浏览器缓存

### 7.2 项目结构
```
Recipe/
├── index.html                     # 主应用（双击打开即可）
├── recipe.json                    # 独立数据文件（51道闽菜 JSON）
└── dev-log/                       # 开发日志文件夹
    ├── requirements.md            # 需求文档（F1-F7）
    ├── tech-spec.md               # 技术规范
    ├── design-spec.md             # 设计规范
    ├── execution-steps.md         # 执行步骤（阶段1-9，21项功能）
    ├── changelog.md               # 版本记录（v1.0.0 → v1.3.0）
    └── conversation-summary.md    # 五轮对话摘要
```

### 7.3 启动步骤
1. 将 `Recipe/` 文件夹放置于任意目录
2. 双击 `index.html`，浏览器自动打开
3. 无需安装任何软件或依赖

### 7.4 常见问题

| 问题 | 解决方法 |
|------|----------|
| 页面样式异常 | 检查网络连接——首次需加载 Tailwind CSS CDN；或等待 CDN 加载完成后刷新 |
| 字体显示异常 | Google Fonts 可能被墙，可修改 CSS 使用系统默认字体 |
| 筛选无结果 | 尝试减少筛选条件——四个维度 AND 组合可能过于严格 |
| 自动补全不出现 | 确保已输入至少 1 个字符；自动补全列表来自 recipes 数据中的食材名 |

---

## 八、反思与改进

### 8.1 我如何看待 Vibe Coding？

Vibe Coding 是一种以自然语言描述需求、由 AI 生成代码的新型开发范式。在本项目中，我作为「需求描述者 + 验收者」，AI 作为「架构师 + 开发者」，形成了一种高效的协作模式。

**我的角色变化**：
- 从「写代码」转变为「写需求、审代码、做决策」
- 关注点从语法细节上移到架构合理性、用户体验、功能完整性
- 需要清晰地表达需求，并能够判断 AI 输出的质量

**AI 的角色**：
- 承担了全部代码编写工作（HTML/CSS/JS + 数据内容）
- 提供了架构建议和技术决策依据
- 在迭代中保持代码一致性和功能完整性

### 8.2 AI 提供了哪些帮助？

1. **快速原型生成**：从需求描述到完整可运行的 index.html，仅一轮对话
2. **数据增强**：为 51 道菜补充具体用量、详细步骤、文化背景——纯人工需要数小时
3. **知识覆盖**：闽菜历史、烹饪技巧、食材用量标准——AI 整合了广泛的领域知识
4. **一致性维护**：多次迭代中保持代码风格、变量命名、UI 规范的统一
5. **调试辅助**：文件损坏时，AI 设计了分步重组方案并用脚本验证完整性

### 8.3 还有哪些不足？

**AI 层面的局限**：
1. **上下文理解有限**：对于长文件（>500 行）的局部操作，AI 可能匹配到错误的编辑位置
2. **工具限制**：Edit 工具在处理大范围替换时效率低，Write 工具受文件大小限制
3. **深度逻辑需人工把关**：复杂交互逻辑（如多维筛选 AND 组合）需要明确的需求描述才能正确实现
4. **无运行时验证**：AI 无法实际运行代码查看效果，依赖用户反馈

**项目层面的改进空间**：
1. URL 状态持久化：刷新不丢失筛选状态，支持分享链接
2. 反向匹配：显示「还差哪些食材就能做这道菜」
3. 收藏功能：localStorage 持久化用户喜欢的菜谱
4. PWA 离线支持：Service Worker 缓存，脱离 CDN 依赖
5. 打印优化：打印菜谱时的专用 CSS 样式

### 8.4 总结

本项目完整展示了 Vibe Coding 从需求到交付的全流程：需求澄清 → 方案设计 → 迭代开发 → 调试修复 → 文档输出。最终交付了一个包含 51 道闽菜的完整筛选网站，实现了 23 项功能需求，并生成了完整的开发文档体系。项目证明：在明确的需求和清晰的验收标准下，AI 辅助开发可以大幅提升效率，同时保持代码质量和功能完整性。

---

**文档版本**：v1.0  
**生成日期**：2026-06-09  
**对应项目版本**：v1.3.0（最终版）
